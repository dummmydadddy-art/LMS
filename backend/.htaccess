<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /backend/
  
  # Forward all requests that are not physical files or directories to index.php
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
